backend:
  name: github
  branch: master
  repo: jeffau13/gatsbyBlog

media_folder: static/img
public_foler: /img

collections:
  - name: "team"
    label: "Team"
    folder: "src/team"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Team Member", name: "title", widget: "string" }
      - { label: "Bio", name: "bio", widget: "markdown" }
